#!/usr/bin/env bash
# Скрипт для установки прав на выполнение скриптов

chmod +x build.sh
chmod +x start.sh
chmod +x parse_db_url.sh
chmod +x fix_db_connection.py

echo "✅ Права на выполнение установлены"
